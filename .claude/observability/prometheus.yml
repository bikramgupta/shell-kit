# Prometheus config for the shared Claude Code + Codex telemetry stack.
# Scrapes the OpenTelemetry Collector's prometheus exporter.

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
